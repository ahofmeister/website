---
title: Supabase RLS Policy Template
publishedAt: 2024-05-26
tags: [supabase, sql]
---


```sql
CREATE POLICY "Allow authenticated users to insert a new entry"
ON table
FOR INSERT
WITH CHECK (auth.uid() IS NOT NULL);

CREATE POLICY "Allow users to read their own entries"
ON table
FOR SELECT
USING (user_id = auth.uid());

CREATE POLICY "Allow users to update their own entries"
ON table
FOR UPDATE
USING (user_id = auth.uid());

CREATE POLICY "Allow users to delete their own entries"
ON table
FOR DELETE
USING (user_id = auth.uid());
